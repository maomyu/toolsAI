# 服务器配置
PORT=3001
NODE_ENV=production

# CORS 配置（前端域名，多个用逗号分隔）
CORS_ORIGIN=http://localhost:5173

# Redis 配置（可选，默认本地）
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_PASSWORD=

# 注意：API Key 在网站「系统设置」页面配置，存储在数据库中
